Firstly, run "npm install" in command line;

Secondly, make sure you have your local gulp.

'gulpfile.js' is basically for test now, so you will see mostly odd thing. 

Run "gulp server" to start the service.